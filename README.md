# News_Bot
Follow this steps to run server on local machine:
1. Clone repository
2. Install Docker, Docker-compose
3. Get file .env from developers and copy to project root directory(to directory news_bot_backend)
4. Build and run container with command "docker-compose up --build"
